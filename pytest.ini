[pytest]
markers =
    smoke: Fast offline checks (no Azure credentials, < 5s)
    integration: Requires Azure credentials and workspace access
    regression: Domain-specific grading and generation tests
testpaths = tests
addopts = -v --tb=short
